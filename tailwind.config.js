/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    './templates/**/*.html', // Include HTML files in the templates directory
    './static/**/*.js',      // Include JavaScript files in the static directory
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
